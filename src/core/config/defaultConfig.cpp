#include "config.h"

const json defaultConfig = {
  {"Macros", {
    {"Pearl Catch", {
      {"Mod Key", "ALT"},
      {"Key", "Q"},
      {"Slots", {
        {"Pearl", "MOUSE_SIDE_BUTTON_FRONT"},
        {"Wind Charge", "X"}
      }}
    }}
  }}
};